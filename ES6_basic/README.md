<p align="center"\>
<img src="https://github.com/Mathieu7483/Aiko78-Photgraphy/blob/main/img/Project%20ES6.png"/>
</p\>

# 🚀 ES6 Basics (Fondamentaux d'ECMAScript 2015)

-----

## 📝 Description du Projet

Ce dépôt est dédié à la maîtrise des fonctionnalités clés introduites dans **ECMAScript 2015 (ES6)**, la version majeure qui a transformé le développement JavaScript. Le projet couvre les nouvelles syntaxes, la gestion des variables, les fonctions et les mécanismes d'itération modernes.

L'objectif est d'abandonner les pratiques plus anciennes (comme l'utilisation de `var`) et d'adopter le code **moderne, lisible et robuste** qui est la norme actuelle dans l'écosystème JavaScript.

-----

## 🎯 Objectifs d'Apprentissage

À la fin de ce projet, vous devez être capable d'expliquer et d'utiliser :

  * **Variables et Portée (`scope`) :** La différence cruciale entre `const`, `let` et l'ancien `var`, ainsi que le concept de **portée de bloc** (*Block-scoped variables*).
  * **Fonctions :** La syntaxe concise des **fonctions fléchées** (*Arrow functions*), les **paramètres par défaut**, et la syntaxe *Rest* pour les arguments.
  * **Opérateurs :** L'opérateur de *Spread* (`...`) pour les collections et les appels de fonctions.
  * **Syntaxe des Chaînes :** L'utilisation des **littéraux de gabarit** (*Template Literals*) pour l'interpolation de chaînes.
  * **Objets :** Les raccourcis de propriété d'objet (*Shorthand syntax*) et la définition de méthodes ES6.
  * **Itération :** L'utilisation des boucles **`for...of`** et la compréhension des **Itérateurs** derrière les collections.

-----

## 💻 Contenu de l'Exercice

Ce projet se concentre sur la réécriture de fonctions et l'implémentation de nouvelles fonctionnalités en utilisant la syntaxe ES6.

| Fichier | Concept ES6 Abordé |
| :--- | :--- |
| `0-constants.js` | **`const`** vs **`let`** et la portée. |
| `1-block-scoped.js` | **Portée de Bloc** (`{}`) avec `let`. |
| `2-arrow.js` | **Fonctions Fléchées** (*Arrow Functions*). |
| `3-default-parameter.js` | **Paramètres par Défaut** dans les fonctions. |
| `4-rest-parameter.js` | **Paramètre Rest** (`...`) pour collecter des arguments. |
| `5-spread-syntax.js` | **Opérateur Spread** (`...`) pour décomposer des itérables. |
| `6-string-hooligan.js` | **Template Literals** (l'interpolation `${}`). |
| `7-getters.js` | **Raccourcis de Propriété** d'objet. |
| `8-getters.js` | Création d'objets sans initialisation préalable. |
| `9-getters.js` | **Méthodes** d'objet ES6. |
| `10-for-of.js` | Boucles **`for...of`** pour l'itération. |
| `11-iterator.js` | Compréhension des **Itérateurs**. |
| `12-create-report-object.js` | Combinaison de fonctionnalités pour un objet complexe. |

-----

## 🛠️ Configuration et Dépendances

Ce projet nécessite une configuration moderne de l'environnement JavaScript pour interpréter et tester le code ES6.

### ⚙️ Prérequis

  * **Environnement d'Exécution :** Node.js **20.x.x** et npm **9.x.x**.
  * **Linting :** **ESLint** (pour la qualité et la conformité du code ES6).
  * **Tests :** **Jest Testing Framework** (pour l'assurance qualité).
  * **Transpilation :** **Babel** (pour gérer les syntaxes modernes).

### 🛠️ Installation

1.  **Clonage du Dépôt :**

    ```bash
    git clone https://github.com/votre-utilisateur/holbertonschool-web_back_end.git
    cd holbertonschool-web_back_end/ES6_basic
    ```

2.  **Installation des Dépendances (Jest, Babel, ESLint) :**

    ```bash
    npm install
    ```

3.  **Exécution du Code (avec Babel) :**

    ```bash
    npm run dev [nom-du-fichier].js
    # Exemple: npm run dev 0-main.js 
    ```

4.  **Tests et Linting :**

    ```bash
    npm run lint       # Analyse le style du code.
    npm test           # Exécute les tests Jest.
    npm run full-test  # Exécute le linting ET les tests.
    ```

-----

## ✒️ Auteur

**Mathieu**

[Mathieu GODALIER](https://github.com/Mathieu7483) - Élève en programmation à la Holberton School
