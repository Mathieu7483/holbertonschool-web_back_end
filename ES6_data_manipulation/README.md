# 📊 ES6 Data Manipulation (Manipulation de Données Moderne en JavaScript)

-----

## 📝 Description du Projet

Ce dépôt se concentre sur les outils et les techniques modernes introduits par **ES6 (ECMAScript 2015)** pour la manipulation efficace des données en JavaScript. Le projet couvre deux piliers majeurs :

1.  **Méthodes d'Itération Fonctionnelle :** Maîtriser les méthodes d'Array comme `map`, `filter`, et `reduce` pour des transformations de données non-mutables.
2.  **Nouvelles Structures de Données :** Utilisation des objets `Set`, `Map`, et des *Typed Arrays* pour des cas d'utilisation spécifiques offrant de meilleures performances ou une sémantique plus claire.

-----

## 🎯 Objectifs d'Apprentissage

À la fin de ce projet, vous devez être capable d'expliquer et d'utiliser :

  * **Méthodes Fonctionnelles d'Array :** `map` (transformation), `filter` (sélection), et `reduce` (agrégation ou accumulation) sur des tableaux.
  * **Sets (`Set`) :** Utilisation de la structure de données `Set` pour stocker des valeurs uniques et effectuer des opérations comme la suppression de doublons.
  * **Maps (`Map`) :** Utilisation de la structure de données `Map` pour stocker des paires clé-valeur avec des clés qui peuvent être de n'importe quel type (contrairement aux objets JavaScript traditionnels).
  * **Typed Arrays :** La structure des tableaux typés pour le travail avec des données binaires ou numériques de manière performante (ex: `Int8Array`, `Float32Array`).

-----

## 💻 Contenu de l'Exercice

Le projet est structuré pour vous faire appliquer les concepts ci-dessus à travers des scénarios courants de manipulation de listes d'objets.

| Fichier | Concept Clé |
| :--- | :--- |
| `0-get_list_students.js` | Création d'une liste d'objets de base. |
| `1-get_list_student_ids.js` | Application de la méthode **`map`** pour extraire des propriétés spécifiques. |
| `2-update_grade_by_city.js` | Application de la méthode **`filter`** combinée à `map` pour une transformation conditionnelle. |
| `3-get_ids_sum.js` | Application de la méthode **`reduce`** pour calculer une somme agrégée. |
| `4-update_student_grades.js` | Combinaison complexe de `map` et `reduce` pour mettre à jour des propriétés d'objets. |
| `5-typed_arrays.js` | Création et manipulation des **Typed Arrays**. |
| `6-set.js` | Utilisation du **`Set`** pour gérer les éléments uniques. |
| `7-has_array_values.js` | Implémentation de fonctions utilitaires pour la structure `Set`. |
| `8-clean_set.js` | Manipulation avancée de `Set` et de chaînes. |
| `9-main.js` | Création et manipulation de la structure de données **`Map`**. |
| `10-main.js` | Implémentation de fonctions utilitaires pour la structure `Map`. |

-----

## 🛠️ Configuration et Dépendances

Ce projet utilise l'environnement de développement JavaScript standard moderne.

### ⚙️ Prérequis

  * **Environnement d'Exécution :** Node.js **20.x.x** et npm **9.x.x**.
  * **Tests :** **Jest Testing Framework**.
  * **Linting :** **ESLint**.

### 🛠️ Installation

1.  **Clonage du Dépôt :**

    ```bash
    git clone https://github.com/votre-utilisateur/holbertonschool-web_back_end.git
    cd holbertonschool-web_back_end/ES6_data_manipulation
    ```

2.  **Installation des Dépendances (Jest, Babel, ESLint) :**

    ```bash
    npm install
    ```

3.  **Tests et Linting :**

      * Exécutez l'ensemble des tests et la vérification de style :
        ```bash
        npm run full-test
        ```

-----

## ✒️ Auteur

**Mathieu**

[Mathieu GODALIER](https://github.com/Mathieu7483) - Élève en programmation à la Holberton School