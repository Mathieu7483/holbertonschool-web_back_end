<p align="center"\>
<img src="https://github.com/Mathieu7483/Aiko78-Photgraphy/blob/main/img/Project%20ES6.png"/>
</p\>


# 🎓 ES6 Classes (Programmation Orientée Objet en JavaScript)

-----

## 📝 Description du Projet

Ce projet est une introduction approfondie aux **Classes ES6**, qui offrent une syntaxe beaucoup plus claire et familière pour implémenter l'héritage basé sur le prototype en JavaScript. L'objectif est de maîtriser les fondements de la **Programmation Orientée Objet (POO)** en utilisant les fonctionnalités modernes du langage.

Ce travail couvre la création de classes, l'utilisation des constructeurs, l'ajout de méthodes d'instance et statiques, la mise en œuvre de l'**héritage** (mot-clé `extends`), et l'utilisation des *getters* et *setters* pour le contrôle des propriétés.

-----

## 🎯 Objectifs d'Apprentissage

À la fin de ce projet, vous devez être capable d'expliquer et d'utiliser :

  * **Définition de Classe :** Comment utiliser la syntaxe `class` pour créer un *blueprint* d'objet.
  * **Constructeur et Propriétés :** Définition de la méthode spéciale `constructor` et initialisation des propriétés (y compris les conventions pour les propriétés "privées" comme `_propertyName`).
  * **Méthodes :** Ajout de méthodes d'instance et de **méthodes statiques** (`static`).
  * **Héritage :** Extension d'une classe parente vers une classe enfant à l'aide de `extends` et appel du constructeur parent via **`super()`**.
  * **Encapsulation :** Utilisation des accesseurs **`get`** (getters) et **`set`** (setters) pour contrôler l'accès et la modification des propriétés.
  * **Concepts Avancés :** Introduction aux `Symbol` et au **métaprogramming** de base (Tâche 8).

-----

## 💻 Contenu de l'Exercice

Ce projet est une série d'implémentations de classes qui simulent des objets du monde réel (salles de classe, cours, bâtiments, voitures) pour appliquer les principes de la POO.

| Fichier | Concept Clé |
| :--- | :--- |
| `0-classroom.js` | **Définition de Classe** et constructeur simple. |
| `1-make_classrooms.js` | Instanciation de multiples objets d'une classe. |
| `2-currency.js` | Implémentation de **Getters** et **Setters** pour la validation/mise en forme. |
| `3-currency.js` | Ajout de méthodes d'instance et de méthodes statiques. |
| `4-pricing.js` | Importation et utilisation d'une classe dans une autre. |
| `5-building.js` | Classe de base pour l'héritage. |
| `6-sky_high.js` | **Héritage (`extends`)** et utilisation de `super()`. |
| `7-airport.js` | Utilisation des *getters* pour retourner des valeurs calculées. |
| `8-hclass.js` | Utilisation des **Symboles** pour les propriétés uniques (*Metaprogramming*). |
| `9-hoisting.js` | Compréhension du *hoisting* de fonction versus de classe. |
| `10-car.js` | Implémentation des *getters* et méthodes d'affichage. |
| `11-evcar.js` | **Héritage complexe** avec surcharge et gestion des propriétés spécifiques. |

-----

## 🛠️ Configuration et Dépendances

Ce projet utilise l'environnement de développement JavaScript standard moderne.

### ⚙️ Prérequis

  * **Environnement d'Exécution :** Node.js **20.x.x** et npm **9.x.x**.
  * **Tests :** **Jest Testing Framework**.
  * **Linting :** **ESLint**.

### 🛠️ Installation

1.  **Clonage du Dépôt :**

    ```bash
    git clone https://github.com/votre-utilisateur/holbertonschool-web_back_end.git
    cd holbertonschool-web_back_end/ES6_classes
    ```

2.  **Installation des Dépendances (Jest, Babel, ESLint) :**

    ```bash
    npm install
    ```

3.  **Tests et Linting :**

      * Exécutez l'ensemble des tests et la vérification de style :
        ```bash
        npm run full-test
        ```

-----

## ✒️ Auteur

**Mathieu**

[Mathieu GODALIER](https://github.com/Mathieu7483) - Élève en programmation à la Holberton School
